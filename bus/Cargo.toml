[package]
name = "oskey-bus"
version = "0.0.1"
edition = "2021"
description = ""
license = ""

[dependencies]
anyhow = { version = "1.0", default-features = false }
prost = { version = "0.14", default-features = false, features = ["derive"] }

[build-dependencies]
prost-build = "0.14"
